name: binning
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python
  - pip
  - numpy
  - scikit-learn
  - biopython
  - tqdm
  - pip:
      - torch
      - transformers==4.50.3
      - einops
      - matplotlib
      - seaborn
      - pandas
      - scikit-image
