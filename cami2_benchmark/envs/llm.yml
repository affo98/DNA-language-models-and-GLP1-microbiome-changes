name: llm
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python
  - pip
  - numpy
  - scikit-learn
  - biopython
  - tqdm
  - pip:
      - torch
      - transformers
      - einops
      - matplotlib
      - pandas
      - scikit-image
